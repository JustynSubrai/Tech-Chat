<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Chat</title>
  <link rel='stylesheet' href='https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css'>
  <link rel="stylesheet" href="/public/css/style.css" type="text/html">
</head>

<body>
  <div class="wrapper">
    <header>
      <section class="jumbotron jumbotron-fluid bg-dark" style="height: 230px;">
  <div class="d-flex justify-content-center" >
        <h1 class="p-10">
          <a href="/" class="font-weight-lighter text-info text-decoration-none m-5">Tech Chat</a>
        </h1>
        <nav class="navbar" role="navigation" aria-label="main navigation">
          {{#if loggedIn}}
          <a class="nav-item btn btn-outline-info my-2 my-sm-0 mr-5" href="/dashboard">dashboard</a>
          <button id="logout" class="nav-item btn btn-outline-info my-2 my-sm-0 mr-5">Logout</button>
          {{else}}
          <a href='/login' class="nav-item btn btn-outline-info my-2 my-sm-0 mr-5">Login</a>
          {{/if}}
        </nav>
    </div>
      </section>
    </header>
    <main>
      {{{ body }}}
    </main>

  </div>

  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>